import React from 'react';
import { game } from '../patterns/structural/flyweight';

export default function FlyweightPage(): JSX.Element {
  console.log(game);
  return (
    <div>
      <h1>Flyweight</h1>
    </div>
  );
}
